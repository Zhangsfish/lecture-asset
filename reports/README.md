# Codex delivery evidence

每阶段每轮一个目录，例如 `reports/S00/round-01/`。

至少包含 DELIVERY.md、ENVIRONMENT.md、TEST_RESULTS.json；小体积脱敏日志/合成截图可放 evidence/。大产物使用实际CI artifact并保留文本摘要和hash。

不上传真实讲座照片、OCR、GPS、照片ID、个人文件路径、Apple账户、UDID、证书、配置描述文件或签名秘密。模板不是执行证据；NOT_RUN不能因为“应该会通过”而改为PASS。
